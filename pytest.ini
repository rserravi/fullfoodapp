[pytest]
addopts = -q
testpaths = tests
filterwarnings =
    ignore::DeprecationWarning
    ignore::UserWarning
markers =
    integration: tests que interactúan con servicios externos reales (p.ej. Azure OpenAI)
